# scope.vim

File detection, syntax highlighting, and formatting integration for the Scope programming language.

Formatting is provided via `scopefmt` on save. Options:

- `g:scope_fmt_autosave` (default `1`)
- `g:scope_fmt_parse_errors` (default `1`)
- `g:scope_fmt_max_window_height` (default `5`)
